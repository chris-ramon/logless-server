- name: Test
  service: app
  encrypted_dockercfg_path: dockercfg.encrypted
  steps: 
  - type: serial
    steps:
    - service: install
      command: npm install
    - service: test
      command: npm test 

#- name: DockerHub push
#  type: push
#  service: app
#  tag: master
#  image_name: xappmedia/logless-server
#  image_tag: "{{.Branch}}.dev"
#  registry: https://index.docker.io/v1/
#  encrypted_dockercfg_path: dockercfg.encrypted

