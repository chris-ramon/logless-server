- name: Test
  service: app
  command: npm test
  encrypted_dockercfg_path: dockercfg.encrypted

- name: DockerHub push
  type: push
  service: app
  tag: master
  image_name: node:6.3.0
  image_tag: "{{.Branch}}.logless-server"
  registry: https://index.docker.io/v1/
  encrypted_dockercfg_path: dockercfg.encrypted

